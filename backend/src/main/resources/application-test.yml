spring:
  datasource:
    url: jdbc:h2:mem:synapse;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  cache:
    type: simple
app:
  security:
    jwt-secret: test-secret-test-secret-test-secret-test-secret
    secure-cookies: false
  storage:
    root: ${java.io.tmpdir}/synapse-hea-test-uploads
  mail:
    enabled: false
